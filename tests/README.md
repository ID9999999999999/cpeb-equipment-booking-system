# Tests

This directory can hold project-level test notes and API request examples.

Executable backend tests should live under `backend/test/` or near backend source files as `*.spec.ts`.

Executable Flutter tests should live under `app/test/`.

Recommended project-level tests:

- repository structure check;
- booking overlap examples;
- API smoke requests;
- config loading checks;
- evidence of test output.
