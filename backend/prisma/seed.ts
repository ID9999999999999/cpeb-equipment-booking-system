// Demo seed placeholder.
// Add fake users and fake equipment only.
// Never seed real student or university private data.

async function main() {
  console.log('Seed script placeholder');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
